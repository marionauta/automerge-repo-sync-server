{
  "name": "@marionauta/automerge-repo-sync-server",
  "version": "0.2.8",
  "main": "src/index.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "bun build --compile ./src/index.ts --outfile sync-server",
    "start": "bun --watch ./src/index.ts"
  },
  "dependencies": {
    "@automerge/automerge-repo": "^1.2.1",
    "@automerge/automerge-repo-network-websocket": "^1.2.1",
    "automerge-repo-storage-bun-sqlite": "^0.1.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.4"
  }
}
